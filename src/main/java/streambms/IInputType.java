package streambms;

public interface IInputType
{
  String getInput(); // input type could reading required number of samples from console or
                     // reading file path as input and process
}
